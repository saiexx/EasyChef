# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EasyChef' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EasyChef

    pod 'Firebase/Core'
    pod 'Firebase/Auth'
    pod 'Firebase/Firestore'
    pod 'Firebase/Storage'
    
    pod 'Kingfisher', '~> 5.0'
    

  target 'EasyChefTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EasyChefUITests' do
    # Pods for testing
  end

end
